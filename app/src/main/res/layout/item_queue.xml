<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_queue_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="@dimen/spacing_small"
    android:paddingTop="8dp"
    android:paddingEnd="@dimen/spacing_small"
    android:paddingBottom="8dp">

    <!-- Reorder Drag Handle -->
    <ImageView
        android:id="@+id/iv_queue_handle"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_sort"
        app:tint="?attr/colorOutline" />

    <!-- Album Art Thumbnail -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iv_queue_thumbnail"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="@dimen/spacing_small"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_album_placeholder"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.SongsPlayer.Small" />

    <!-- Title & Artist -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_medium"
        android:layout_marginEnd="@dimen/spacing_small"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_queue_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Track Title"
            android:textColor="?attr/colorOnSurface"
            android:textSize="@dimen/text_body"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_queue_artist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Artist Name"
            android:textColor="?attr/colorOutline"
            android:textSize="@dimen/text_small" />
    </LinearLayout>

    <!-- Remove from Queue Button -->
    <ImageButton
        android:id="@+id/btn_queue_remove"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Remove"
        android:src="@drawable/ic_clear"
        app:tint="?attr/colorOutline" />

</LinearLayout>
