<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_song_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:paddingStart="12dp"
    android:paddingTop="8dp"
    android:paddingEnd="8dp"
    android:paddingBottom="8dp">

    <!-- Thumbnail Card with Duration Badge Overlay -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_thumbnail"
        android:layout_width="112dp"
        android:layout_height="66dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="2dp"
        app:strokeWidth="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/iv_song_thumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/bg_album_placeholder" />

            <!-- Active Playing Indicator Overlay in top-start corner -->
            <ImageView
                android:id="@+id/iv_playing_indicator"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="top|start"
                android:layout_margin="4dp"
                android:background="@drawable/bg_circle_icon"
                android:backgroundTint="@color/primary"
                android:padding="3dp"
                android:src="@drawable/ic_volume_up"
                android:visibility="gone"
                app:tint="#FFFFFF" />

            <!-- Duration Badge Overlay in bottom-right corner -->
            <TextView
                android:id="@+id/tv_duration_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:layout_margin="4dp"
                android:background="#D9000000"
                android:paddingStart="5dp"
                android:paddingTop="2dp"
                android:paddingEnd="5dp"
                android:paddingBottom="2dp"
                android:text="3:45"
                android:textColor="#FFFFFF"
                android:textSize="10sp"
                android:textStyle="bold" />
        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Title & Artist (Multi-line) -->
    <LinearLayout
        android:id="@+id/layout_song_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="6dp"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="@id/card_thumbnail"
        app:layout_constraintBottom_toBottomOf="@id/card_thumbnail"
        app:layout_constraintStart_toEndOf="@id/card_thumbnail"
        app:layout_constraintEnd_toStartOf="@id/iv_favorite">

        <TextView
            android:id="@+id/tv_song_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:lineSpacingExtra="2dp"
            android:text="Song Title Name • Complete Mix"
            android:textColor="?attr/colorOnSurface"
            android:textSize="13.5sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_song_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Artist Name • 2.4M views"
            android:textColor="?attr/colorOutline"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Favorite Icon -->
    <ImageButton
        android:id="@+id/iv_favorite"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Favorite"
        android:src="@drawable/ic_favorite_border"
        app:tint="?attr/colorOutline"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btn_song_more" />

    <!-- 3-Dots More Options Menu Icon -->
    <ImageButton
        android:id="@+id/btn_song_more"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="More Options"
        android:src="@drawable/ic_more_vert"
        app:tint="?attr/colorOutline"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
