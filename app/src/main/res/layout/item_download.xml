<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_download_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="@dimen/spacing_small"
    android:paddingTop="8dp"
    android:paddingEnd="@dimen/spacing_small"
    android:paddingBottom="8dp">

    <!-- Multi-select CheckBox (visible in selection mode) -->
    <CheckBox
        android:id="@+id/cb_download_select"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <!-- Album Art Thumbnail -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iv_download_thumbnail"
        android:layout_width="@dimen/album_art_small"
        android:layout_height="@dimen/album_art_small"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_album_placeholder"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.SongsPlayer.Small" />

    <!-- Title & Details -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_medium"
        android:layout_marginEnd="@dimen/spacing_small"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_download_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Song Title"
            android:textColor="?attr/colorOnSurface"
            android:textSize="@dimen/text_body"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_download_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="5.4 MB • Completed"
            android:textColor="?attr/colorOutline"
            android:textSize="@dimen/text_small" />

        <!-- Linear Progress (visible during active download) -->
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progress_download"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_marginTop="4dp"
            android:visibility="gone"
            app:indicatorColor="@color/primary"
            app:trackColor="?attr/colorSurfaceVariant" />
    </LinearLayout>

    <!-- Download Status Icon / Delete Button -->
    <ImageButton
        android:id="@+id/btn_download_action"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Action"
        android:src="@drawable/ic_check"
        app:tint="@color/success" />

</LinearLayout>
